* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Header container */
#main-header {
    background-color: #87ebdd9d; /* Customize as needed */
    padding: 15px 0;
}

.container {
    max-width: 1000px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

/* Logo */
.logo {
    text-decoration: none;
}

.logo img {
    height: 50px; /* Adjust as needed */
}

/* Navigation */
.navigation {
    list-style: none;
    display: flex;
}

.navigation li {
    margin: 0 15px;
    display: inline-block;
}

.navigation a {
    text-decoration: none;
    color: #121658;
    padding: 10px 15px;
    font-weight: bold;
}

.navigation a:hover {
    color: #152435; /* Customize on hover */
}

/* Dropdown */
.dropdown {
    position: relative;
}

.dropdown-content {
    display: none;
    position: absolute;
    background-color: #f1f1f1;
    min-width: 160px;
    box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
    z-index: 1;
}

.dropdown:hover .dropdown-content {
    display: block;
}

.dropdown-content a {
    color: black;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    text-align: left;
}

/* Search bar */
.search-bar {
    display: flex;
    align-items: center;
}

.search-bar input[type="text"] {
    padding: 8px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 1
}
main {
    padding: 2rem;
  }
  
  .images {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
  
  .images img {
    width: 20rem;
    margin: 1rem;
  }
  
  .cards {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
  
  .card {
    width: 20rem;
    margin: 1rem;
    border: 1px solid #ccc;
    border-radius: 5px;
  }
  
  .card img {
    width: 100%;
  }
  
  .card-content {
    padding: 1rem;
  }
  
  .card-content h3 {
    margin-bottom: 0.5rem;
  }
footer {
    background-color: #87ebdd9d; /* Customize as needed */
    padding: 30px 0;
  }
  
  .container {
    max-width: 1000px;
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
  }
  
  .footer-content {
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 20px;
  }
  
  .footer-col {
    flex: 1 0 30%;
    padding: 15px;
  }
  
  .footer-col h2 {
    margin-bottom: 10px;
  }
  
  .footer-col ul {
    list-style: none;
    padding: 0;
  }
  
  .footer-col li {
    margin-bottom: 5px;
  }
  
  .footer-col a {
    text-decoration: none;
    color: black;
  }
  
  .footer-col a:hover {
    color: #0099ff; /* Customize hover color */
  }
  
  .social-media li {
    display: inline-block;
    margin-right: 10px;
  }
  
  
  
  .footer-copyright {
    text-align: center;
    margin-top: 20px;
  }
  
  .footer-copyright p {
    font-size: 0.8em;
    margin: 0;
  }
